import { PRODUCT_NAME } from "../../brand.js";

/**
 * Default approval message sent after channel pairing succeeds.
 */
export const PAIRING_APPROVED_MESSAGE = `✅ ${PRODUCT_NAME} access approved. Send a message to start chatting.`;
